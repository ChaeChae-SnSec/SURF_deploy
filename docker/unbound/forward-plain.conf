# 평문 업스트림. TLS 를 가로채는 네트워크에서 쓴다.
#
# 클라이언트와 SURF 사이 구간은 DoH 로 암호화되지만, SURF 와 상용 리졸버 사이는
# 평문이다. 그 구간을 볼 수 있는 위치에 있는 관찰자에게는 질의가 노출된다.
# 네트워크가 허용하면 forward-dot.conf 를 쓰는 편이 낫다.
forward-zone:
    name: "."
    forward-addr: 1.1.1.1
    forward-addr: 1.0.0.1
    forward-addr: 8.8.8.8
